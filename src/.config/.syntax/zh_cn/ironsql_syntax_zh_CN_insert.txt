[*] 'INSERT' 操作语法:

[*] 使用 'INSERT' 语句插入数据:
[*] insert into <table_name> (<field1>, <field2>, ...) values (<value1>, <value2>, ...);

[*] 注意!
[*] 在使用'INSERT' 语句插入数据时,应当注意以下几点:
    必须先选择数据库(使用USE语句)
    表必须存在
    字段名和值的数量必须匹配
    字段值的类型必须与表结构定义的类型一致