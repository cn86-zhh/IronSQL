# WELCOME TO HERE

- 目录`.config` 用于存储配置文件。
- 目录`.config/.win32` 用于存储Windows平台的配置文件。
- 目录`.config/.linux` 用于存储Linux平台的配置文件。
- `ironsql_syntax_help_en_US.txt` 与 `ironsql_syntax_help_zh_CN.txt` 他们遵循IronSQL的语法标准文档,
  用于存储语法帮助文档。

- 这些文件主要用于初始化加载IronSQL的配置文件。
  包括一些全局设置，局部设置及一些平台特定的设置。
