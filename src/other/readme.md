# 模块 `other`

- `.../other/go`
  - go 语言处理模块, 此模块为预留模块, 用于处理用户的为并发传输.

- `.../other/rust`
  - rust 语言处理模块, 此模块只能包含safe相关, 不会将unsafe用于此模块, 这不符合
    设计标准。
    此模块为预设模块! 可能会包含一些区块链的处理, 也可能包含其他rust特有的功能处理.
