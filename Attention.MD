# zh_CN

- 我们不建议使用来自MSVC的编译器与CMakeLists.txt
- 我们的标准是基于GNU/MinGW的G++编译器,而不是MSVC的标准.
- 所以, 当使用MSVC的编译器时, 可能会遇到一些问题, 甚至会导致编译失败.
- 注意: CMakeLists.txt 与 Makefile 编译文件均由 AI 生成.
- 如果希望保持简洁性, 我们建议使用 makec++.sh 编译.

# en_US

- We do not recommend using the compiler from MSVC with CMakeLists.txt
- Our standard is based on the GNU/MinGW G++ compiler, not the MSVC standard.
- So, when using the MSVC compiler, you may encounter some problems, or even cause compilation to fail.
- Note: CMakeLists.txt and Makefile are generated by AI.
- If you want to keep the simplicity of the code, we suggest using makec++.sh to compile.

---

# zh_CN: 2026-03-26 AM 12:20

- help模块正在重构，当前无法编译
- 如果需要编译，请在server.cpp和main.cpp中禁用help模块相关代码即可编译

# en_US: 2026-03-26 AM 12:20

- The help module is currently being refactored and cannot be compiled at this time
- If you need to compile, please disable help module related code in server.cpp and main.cpp
